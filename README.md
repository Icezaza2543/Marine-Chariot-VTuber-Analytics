# Marine Chariot VTuber Analytics

Repo name: `marine-chariot-vtuber-analytics`

Production-ready Vite + React + TypeScript dashboard for Marine Chariot VTuber analytics. The app combines the supplied YouTube CSV dataset with optional X social signal data from `https://x.com/MarineChariot`.

## Features

- 3-page dashboard: `Overview`, `Content & Timing`, `Prediction & Strategy`
- Compact filter bar with date range, search, tag, and dropdown multi-selects
- KPI cards, growth trend, yearly comparison, content efficiency quadrant, engagement mix, duration vs engagement, upload heatmap, and sortable top videos
- GitHub-style upload heatmap with square 1:1 cells and single-tone intensity scale
- Forecasting with linear regression + exponential smoothing
- X social signal layer: post engagement, reach proxy, cross-promotion rate, hashtags, and video/post matching
- Thai AI Insight Engine for content, timing, retention, X promotion, and next-month strategy
- Vercel-ready config with static output and optional build-time X data refresh

## Stack

- Vite + React + TypeScript SPA
- Tailwind CSS v4 + daisyUI theme tokens
- Chart.js + annotation, zoom, and datalabels plugins
- Recharts for forecast/category charts
- Zustand for dashboard UI state
- React Hook Form + Zod for filters
- date-fns for date grouping and range math
- Papa Parse for CSV loading
- Framer Motion for UI motion

## Data Files

```txt
public/data/marine-ch-data.csv
public/data/marine-x-posts.json
```

`marine-ch-data.csv` is the original YouTube dataset.

`marine-x-posts.json` is generated by the X fetch script. The committed file starts as an empty cache so local and Vercel builds never fail if an X token is not configured.

## Local Setup

```bash
npm install
npm run dev
```

Open:

```txt
http://127.0.0.1:5173/
```

## Fetch X Data

Create `.env.local`:

```bash
X_BEARER_TOKEN=your_x_api_v2_bearer_token
X_USERNAME=MarineChariot
X_MAX_POSTS=100
X_MAX_PAGES=3
```

Fetch and update the local cache:

```bash
npm run fetch:x
```

Optional mode never fails the build when the token is missing:

```bash
npm run fetch:x:optional
```

The script uses the X API v2 user lookup and user timeline endpoints, then writes normalized public metrics to `public/data/marine-x-posts.json`.

## Build

```bash
npm run build
```

Vercel build command:

```bash
npm run build:vercel
```

`build:vercel` runs `fetch:x:optional` first. If `X_BEARER_TOKEN` exists in Vercel environment variables, fresh X posts are pulled during deployment. If not, the app deploys with the existing cache.

## Vercel Deploy

This repo includes `vercel.json`:

- Framework: Vite
- Build command: `npm run build:vercel`
- Output directory: `dist`
- Static caching for `/assets/*`
- Short cache + stale-while-revalidate for `/data/*`

Recommended Vercel environment variable:

```txt
X_BEARER_TOKEN
```

Preview deploy:

```bash
npm run deploy:preview
```

Production deploy:

```bash
npm run deploy:prod
```

For Git integration, connect this repo to Vercel and push to the production branch. Vercel will use `vercel.json` automatically.

## QA

```bash
npm run check
npm run build
```

Known build note: the bundle-size warning is expected because the dashboard intentionally ships Chart.js, Chart.js plugins, and Recharts together.
